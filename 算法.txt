
二叉树的广度优先遍历，利用队列结构
二叉树的深度优先遍历，利用栈结构


两个相同的数做^（异或）操作，结果为0
	&：同1得1
	|：有1得1
	^：不同为1


拓扑排序

动态规划

LRU缓存 get put O（1）
    自定义一个双向链表，记录链表的头和尾
    Map<Integer,Node>存储key和对应的node
    get元素，获取node，改变node前后节点的关系，然后把node放到双向链表的头部


快慢指针算法

