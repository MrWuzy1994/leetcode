
二叉树的广度优先遍历，利用队列结构
二叉树的深度优先遍历，利用栈结构


两个相同的数做^（异或）操作，结果为0
	&：同1得1
	|：有1得1
	^：不同为1


拓扑排序

动态规划

LRU缓存 get put O（1）
    自定义一个双向链表，记录链表的头和尾
    Map<Integer,Node>存储key和对应的node
    get元素，获取node，改变node前后节点的关系，然后把node放到双向链表的头部

滚动数组

快慢指针算法

双指针
    经典蓄水池方法

要求不能使用乘除法、for、while、if、else、switch、case等关键字及条件判断语句（A?B:C）。
    可以考虑
        可以使用  && 语句 ，如果前半部分条件为false，则后半部分则不会执行
        可以使用  || 语句 ，如果前半部分条件为true，则后半部分则不会执行



力扣模式识别
    连续子数组问题，使用前缀和解决问题
    一旦出现统计次数，可以考虑散列表
    子问题和原问题具有相同结构，考虑自上而下的递归
    需要移动左右两头的问题可以考虑双指针